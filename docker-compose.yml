version: '3.9'
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: gmcrawler
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  app:
    build: .
    command: sh -c "cd backend && npm run start:docker"
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/gmcrawler
    depends_on:
      - postgres
      - redis
    ports:
      - "3001:3001"

  frontend:
    build: .
    command: sh -c "cd frontend && npm run dev -- --host 0.0.0.0 --port 5173"
    env_file:
      - .env
    depends_on:
      - app
    ports:
      - "5173:5173"

  worker:
    build: .
    command: sh -c "cd backend && npm run dev:docker"
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/gmcrawler
    depends_on:
      - postgres
      - redis

volumes:
  pgdata:
